{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': ''}}) }}
    <div class="row g-3">
        <div class="col-md-8">
            <div class="mb-3">
                {{ form_label(form.nom, null, {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.nom, {'attr': {'class': 'form-control', 'placeholder': 'Ex: Mathématiques'}}) }}
                {{ form_errors(form.nom) }}
            </div>
        </div>
        
        <div class="col-md-4">
            <div class="mb-3">
                {{ form_label(form.coef, null, {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.coef, {'attr': {'class': 'form-control', 'placeholder': 'Ex: 3'}}) }}
                {{ form_errors(form.coef) }}
            </div>
        </div>
    </div>

    <div class="d-flex gap-2 mt-4">
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-check-circle"></i> {{ button_label|default('Enregistrer') }}
        </button>
        <a href="{{ path('app_matiere_index') }}" class="btn btn-secondary">
            <i class="bi bi-x-circle"></i> Annuler
        </a>
    </div>
{{ form_end(form) }}
