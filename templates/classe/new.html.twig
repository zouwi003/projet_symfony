{% extends 'base.html.twig' %}

{% block title %}Nouvelle Classe{% endblock %}

{% block body %}
<div class="page-header">
    <h1><i class="bi bi-plus-circle-fill"></i> Créer une nouvelle classe</h1>
    <p class="text-muted mb-0">Ajoutez une nouvelle classe à l'établissement</p>
</div>

<div class="card">
    <div class="card-body p-4">
        {{ include('classe/_form.html.twig') }}
    </div>
</div>

<div class="mt-3">
    <a href="{{ path('app_classe_index') }}" class="btn btn-secondary">
        <i class="bi bi-arrow-left"></i> Retour à la liste
    </a>
</div>
{% endblock %}
